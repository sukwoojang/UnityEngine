using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tutorial : MonoBehaviour
{
    int x = 100;
    /* sbyte&byte 1byte 
     * short 2byte
     * integer 4byte
     * long 8byte
     */

    /* float : 실수 자료형
     * double : 실수 자료형
     * decimal : 실수 자료형
     */

    /* string : "" 문자형 자료형
     * char : '' 한글자만 가능 unicode 기억
     */

    // Start is called before the first frame update
    void Start()
    {
        print(x);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
